# 物理边界分段算法 — 上线执行手册

> 将剩余待办拆分为 3 条独立轨道，可分别在单独终端窗口中并行执行。

## 前置：已完成项

- ✅ Phase 1: Python 原型验证（23,999 条曲线统计符合物理模型）
- ✅ Phase 2: C# 核心算法重写（FeatureExtractor / CurrentFeatureExtractor / StandardCurveBuilder / MainForm / DriftEstimator）
- ✅ Phase 4: CONTEXT.md 文档更新为 v2 值
- ✅ `diag_reference_check.py` 和 `physeg_prototype.py` 已使用物理边界检测

## 待办总览

```
轨道 1: Python 脚本同步（5 个脚本，无需编译，~30min）
  1a. current_baseline_ref_check.py   — extract_phase() 改物理边界
  1b. dryrun_cr001.py                 — extract() 改物理边界
  1c. verify_standard_curve.py        — extract_features() 改物理边界
  1d. rebuild_baselines_by_direction.py — extract_features() 改物理边界
  1e. D8-6_generate_visual.py         — extract_features() 改物理边界
  1f. 运行验证（golden / baseline / dryrun）

轨道 2: C# 编译 + 数据重建（需 VS2010，~1-2h）
  2a. 更新 DiagTool/Program.cs 黄金夹具期望值
  2b. 编译解决方案
  2c. DiagTool selftest
  2d. DiagTool baseline（全量重建）
  2e. 重建标准曲线

轨道 3: C# 单元测试（可选，时间允许时做，~1h）
  3a. D8Tests GetPointAlpha 调用签名更新
  3b. D8Tests/D9Tests MakePowerCurve/MakeTestStandardCurve 硬编码边界更新
```

**轨道 1 和 轨道 2 完全独立，可同时启动。** 轨道 3 依赖轨道 2 编译通过。

---

## 轨道 1: Python 脚本同步

### 通用修改模式

所有需要修改的脚本共享同一模式：用 `physeg_prototype` 的两个检测函数替代硬编码偏移量，保留退化回退路径。

```python
from physeg_prototype import detect_unlock_end, detect_contact_and_lock

# 旧: ul_seg = v[sp+2 : sp+14]
# 新:
unlock_end = detect_unlock_end(v, sp, last)
if unlock_end is not None and unlock_end > sp + 1:
    ul_seg = v[sp + 2 : unlock_end + 1]
else:
    ul_seg = v[sp + 2 : max(sp + 14, int(last * 0.5))]  # 退化

# 旧: conv = v[sp+20 : last-40]
# 新:
lock_start, lock_peak = detect_contact_and_lock(v, last)
if lock_start is None:
    lock_start = last - 40 if last > 50 else last  # 退化
conv_start = (unlock_end + 1) if unlock_end is not None else (sp + 14)
conv = v[conv_start : lock_start]

# 旧: lock_seg = v[last-40 : last-22]
# 新:
if lock_peak is not None and lock_start is not None and lock_peak > lock_start:
    # 物理锁闭段: [lock_start, lock_end+1]
    # lock_end = 从 lock_peak 向后搜索直到功率回到锁闭前水平
    ...
else:
    # 退化: lock_seg = v[last-40 : last-22]
```

### 1a. `current_baseline_ref_check.py`

**路径**: `02_source/tools/current_baseline_ref_check.py`  
**函数**: `extract_phase(values)` (L35-115)  
**当前**: 纯硬编码偏移量 `si+14`, `si+20`, `ae-40`, `ae-22`

将 L35-115 的 `extract_phase()` 替换为物理边界版。参考实现见 `diag_reference_check.py` 的 `extract()`（该函数已正确使用 physeg_prototype）。注意 `extract_phase` 返回 dict 的 key 需增加 `unlockEnd` 和 `lockStart` 字段，同时更新 `_empty_phase()` 返回对应的默认值。

### 1b. `dryrun_cr001.py`

**路径**: `02_source/tools/dryrun_cr001.py`  
**函数**: `extract(v)` (L34-73)  
**当前**: 纯硬编码偏移量

`dryrun_cr001.py` 的 `extract()` 与 `diag_reference_check.py` 的 `extract()` 几乎完全相同（只是缺少物理边界检测）。最简做法：直接复用 `diag_reference_check.py` 的 `extract`，删除 `dryrun_cr001.py` 中的重复定义。

```python
# 在 dryrun_cr001.py 顶部添加:
from diag_reference_check import extract

# 然后删除 L34-73 的 def extract(v): ... 整个函数
```

### 1c. `verify_standard_curve.py`

**路径**: `04_tests/scripts/verify_standard_curve.py`  
**函数**: `extract_features(values)` (L47-140)  
**当前**: 纯硬编码偏移量

将 `extract_features()` 中的硬编码替换为 physeg_prototype 调用。注意此脚本位于 `04_tests/scripts/`，导入 `physeg_prototype` 需要调整 sys.path：

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "02_source" / "tools"))
from physeg_prototype import detect_unlock_end, detect_contact_and_lock
```

> 此脚本也会被 `batch_build_standard_curves.py` 导入，修改后自动生效。

### 1d. `rebuild_baselines_by_direction.py`

**路径**: `04_tests/scripts/rebuild_baselines_by_direction.py`  
**函数**: `extract_features(values)` (L74-140)  
**当前**: 纯硬编码偏移量

同 1c 的修改模式和 sys.path 处理。

### 1e. `D8-6_generate_visual.py`

**路径**: `04_tests/scripts/D8-6_generate_visual.py`  
**函数**: `extract_features(values)` (L22-40)  
**当前**: 纯硬编码偏移量（且锁闭段结束位置用 `ae-19` 不同于其他脚本的 `ae-22`）

同 1c 的修改模式。

### 1f. 运行验证

```bash
cd "d:\Vibe coding\04 DCjiance\SwitchMonitor\02_source\tools"

# 金标准夹具（应与 CONTEXT.md §3 一致）
python diag_reference_check.py golden

# 基线表（应与 CONTEXT.md §2.2 一致）
python diag_reference_check.py baseline

# 全量 dryrun（触发率应 1-3%）
python diag_reference_check.py dryrun

# CR001 专项
python dryrun_cr001.py

# 电流基线
python current_baseline_ref_check.py
```

---

## 轨道 2: C# 编译 + 数据重建

### ⚠️ 编译前必检项

`DriftEstimator.cs` 的 `ApplyDrift` 方法之前调用 `GetPointAlpha` 时遗漏了 `unlockEnd` 和 `lockStart` 参数（已修复）。编译前请确认 L133-137 签名正确：

```csharp
// 正确签名（已修复）:
double driftAlpha = StandardCurveBuilder.GetPointAlpha(
    i, si, feat.UnlockEnd, feat.LockStart, ae, n,
    drift.DriftSpike, drift.DriftUnlock, drift.DriftConv,
    drift.DriftLock, drift.DriftTail, hw);
```

### 2a. 更新 DiagTool 黄金夹具期望值

**文件**: `02_source/src/SwitchMonitor.DiagTool/Program.cs`，L115-182

替换四个 `GoldenFixture.Expected` 为 v2 物理边界值（来自 CONTEXT.md §3）：

| 字段 | 夹具A (1-J) | 夹具B (1-X) | 夹具C (4-J超时) | 夹具D (4-X夭折) |
|------|------------|------------|-----------------|-----------------|
| SampleCount | 300 | 220 | 790 | 27 |
| ActiveEnd | 293 | 213 | 783 | 20 |
| DurationSec | 11.76 | 8.56 | 31.36 | 0.84 |
| SpikePeak | 3.392 | 3.294 | 4.353 | 3.373 |
| SpikeIndex | 6 | 6 | 7 | 5 |
| **UnlockEnd** | **111** | **104** | **320** | **11** |
| **LockStart** | **238** | **161** | **-1** | **-1** |
| UnlockMean | 0.286 | 0.254 | 0.572 | 0.216 |
| ConvMean | 0.319 | 0.267 | 0.286 | 0.216 |
| ConvMax | 0.353 | 0.275 | 0.627 | 0.235 |
| StepRatio | 0.922 | 0.971 | 1.185 | 1.0 |
| LockMean | 0.353 | 0.251 | 0.704 | 0.0 |
| TailMean | 0.251 | 0.217 | 0.706 | 0.0 |

同时需要在 selftest 字段检查循环（L221-233 附近）中**增加两行**：

```csharp
fixturePass &= CheckField("UnlockEnd",  actual.UnlockEnd,  fixture.Expected.UnlockEnd,  false, ref allPass);
fixturePass &= CheckField("LockStart",  actual.LockStart,  fixture.Expected.LockStart,  false, ref allPass);
```

### 2b. 编译

```bash
# VS2010 IDE: 打开 02_source/src/SwitchMonitor.sln → Release|x86 → 生成解决方案

# 或命令行 (MSBuild 4.0):
cd "d:\Vibe coding\04 DCjiance\SwitchMonitor\02_source\src"
%WINDIR%\Microsoft.NET\Framework\v4.0.30319\MSBuild.exe SwitchMonitor.sln /p:Configuration=Release /p:Platform=x86
```

> **MSBuild 兼容性**: 如果 MSBuild 4.8 报 XML 命名空间错误，原因是 VS2010 创建的 `.csproj` 文件 `<Project>` 根元素缺少 `xmlns="http://schemas.microsoft.com/developer/msbuild/2003"`。需要在 VS2010 IDE 中编译，或手动添加 xmlns 属性。

### 2c. selftest

```bash
cd "d:\Vibe coding\04 DCjiance\SwitchMonitor\02_source\src\SwitchMonitor.DiagTool\bin\Release"
DiagTool.exe selftest "d:\Vibe coding\04 DCjiance\SwitchMonitor\03_raw_data\sanshuibei_csv"
```

**验收**: 4 个夹具特征 + 诊断断言全部 PASS。

### 2d. 全量基线重建

```bash
DiagTool.exe baseline "d:\Vibe coding\04 DCjiance\SwitchMonitor\03_raw_data\sanshuibei_csv"
```

**验收**: `05_production_data/baselines.json` 和 `current_baselines.json` 更新，8 台 × 2 方向 = 16 组基线值完整，与轨道 1 的 Python baseline 输出一致。

### 2e. 标准曲线重建

```bash
DiagTool.exe build-standard "d:\Vibe coding\04 DCjiance\SwitchMonitor\05_production_data"
```

**验收**: `standard_curves/` 下所有 JSON 文件已更新，`Alpha*` 字段在 0.7-1.3 范围。

---

## 轨道 3: C# 单元测试（可选）

### 3a. D8Tests — GetPointAlpha 签名更新

**文件**: `02_source/src/SwitchMonitor.Tests/D8Tests.cs`  
**问题**: 5 个测试方法（L641-707）使用旧 10 参数签名 `(i, si, ae, n, α...)`，缺少 `unlockEnd, lockStart`

修改每个调用点，在 `si` 之后插入 `unlockEnd` 和 `lockStart` 参数。对于 300 点测试曲线（si=6, ae=293），物理边界可用 `FeatureExtractor.Extract()` 提取，或直接用常量：
- `unlockEnd = 20`（≈ si+14，接近物理边界）
- `lockStart = 253`（≈ ae-40）

### 3b. D8Tests/D9Tests — 曲线生成器更新

**文件**: 
- `D8Tests.cs` L87-151 `MakePowerCurve`
- `D9Tests.cs` L46-105 `MakeTestStandardCurve`

两个函数都使用硬编码 `si+14, si+20, ae-40, ae-22` 生成合成曲线。如果只是测试 alpha 过渡逻辑（与分段算法正交），可以保留这些合成曲线的固定边界——测试关注的是"给定边界后 alpha 是否正确"，而非"边界检测是否正确"。但如果要端到端测试，需改为用物理边界检测提取实际边界后再验证。

**建议**: 轨道 3 可先跳过，优先完成轨道 1+2 的上线工作。

---

## 执行时序

```
T+0     同时启动轨道 1 和轨道 2
        ├─ 轨道 1: 1a→1b→1c→1d→1e→1f（~30min，纯 Python）
        └─ 轨道 2: 2a→2b→2c→2d→2e（~1-2h，需编译调试）

T+1h    轨道 1 完成 → 输出 golden/baseline/dryrun 结果
T+2h    轨道 2 完成 → C# selftest 与 Python golden 交叉验证
        （可选）轨道 3: 单元测试更新
```

## 验证清单

- [ ] 1a: `current_baseline_ref_check.py` extract_phase() 已更新
- [ ] 1b: `dryrun_cr001.py` extract() 已改为复用 diag_reference_check
- [ ] 1c: `verify_standard_curve.py` extract_features() 已更新
- [ ] 1d: `rebuild_baselines_by_direction.py` extract_features() 已更新
- [ ] 1e: `D8-6_generate_visual.py` extract_features() 已更新
- [ ] 1f: Python golden 输出与 CONTEXT.md §3 一致（容差 ±0.002）
- [ ] 1f: Python baseline 输出与 CONTEXT.md §2.2 一致
- [ ] 1f: Python dryrun 触发率 1-3%
- [ ] 2a: `Program.cs` 黄金夹具 v2 值 + UnlockEnd/LockStart 检查已添加
- [ ] 2b: C# 编译成功（Release|x86，0 错误）
- [ ] 2c: `DiagTool selftest` 4/4 PASS
- [ ] 2d: `DiagTool baseline` 全量重建成功
- [ ] 2e: 标准曲线重建成功
- [ ] 交叉验证: C# selftest/baseline 与 Python golden/baseline 数值一致
